FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Create reports directory
RUN mkdir -p tests/reports

CMD ["pytest", "-v", "--tb=short", "--html=tests/reports/test_report.html"]