
services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000

  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    depends_on:
      - app
    volumes:
      - ./tests/reports:/app/tests/reports
    environment:
      - APP_URL=http://app:5000
    command: pytest -v --html=tests/reports/test_report.html

networks:
  default:
    name: hello-world-network